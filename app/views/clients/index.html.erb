<h1>Strzelnica</h1>
<h2>Clients</h2>

<table id="clients">
  <% @clients.each do |client| %>
    <tr>
        <td><%= link_to client.name, client %> <%= client.surname %> </td>
    </tr>
  <% end %>
</table>

<br><%= link_to "New client", new_client_path if authenticated? %>
