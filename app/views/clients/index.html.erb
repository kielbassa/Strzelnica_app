<h1>Clients</h1>

<%= link_to "New client", new_client_path if authenticated? %>

<div id="clients">
  <% @clients.each do |client| %>
    <div>
        <%= link_to client.name, client %>
    </div>
  <% end %>
</div>
